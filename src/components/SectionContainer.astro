---
const { class: className, id } = Astro.props
---

<section
  id={id}
  data-section={id}
  class={`section ${className} relative w-full mx-auto container lg:max-w-4xl md:max-w-2xl py-16 md:py-24 flex flex-col gap-y-8 md:gap-y-8`}
>
  <slot />
</section>
