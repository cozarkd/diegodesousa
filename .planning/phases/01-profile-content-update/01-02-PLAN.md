---
phase: 01-profile-content-update
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/i18n/pt.json
  - src/i18n/gl.json
autonomous: true

must_haves:
  truths:
    - "SEO title in pt and gl positions Diego as Full-Stack Developer & Product Designer in each language"
    - "SEO description in pt and gl mentions both service lines in culturally adapted copy"
    - "Hero headline in pt and gl introduces Diego with new positioning"
    - "About section in pt and gl presents design-first narrative with all required elements, reading as native copy"
    - "Contact form help placeholder in pt and gl includes SaaS, applications, and dashboards"
    - "All 4 JSON files (es, en, pt, gl) have identical key structure and valid JSON syntax"
    - "npm run build succeeds without errors"
  artifacts:
    - path: "src/i18n/pt.json"
      provides: "European Portuguese localization with updated profile content"
      contains: "Programador Full-Stack"
    - path: "src/i18n/gl.json"
      provides: "Galician localization with updated profile content"
      contains: "Desenvolvedor Full-Stack"
  key_links:
    - from: "src/i18n/pt.json"
      to: "src/i18n/es.json"
      via: "identical key structure for fallback merge"
      pattern: "SEO_TITLE_1|SEO_DESCRIPTION|HERO|ABOUTSECTION|CONTACTSECTION"
    - from: "src/i18n/gl.json"
      to: "src/i18n/es.json"
      via: "identical key structure for fallback merge"
      pattern: "SEO_TITLE_1|SEO_DESCRIPTION|HERO|ABOUTSECTION|CONTACTSECTION"
---

<objective>
Localize Portuguese (pt.json) and Galician (gl.json) i18n files with updated profile positioning, then validate all 4 language files for JSON syntax, key structure consistency, and successful build.

Purpose: Complete the 4-language profile update with European Portuguese and professional Galician localizations, and validate the entire content update works correctly.
Output: Updated pt.json and gl.json with culturally adapted profile content; all 4 files validated.
</objective>

<execution_context>
@/Users/mini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-profile-content-update/01-CONTEXT.md
@.planning/phases/01-profile-content-update/01-RESEARCH.md
@.planning/phases/01-profile-content-update/01-01-SUMMARY.md
@src/i18n/es.json
@src/i18n/en.json
@src/i18n/pt.json
@src/i18n/gl.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Localize pt.json — European Portuguese profile content</name>
  <files>src/i18n/pt.json</files>
  <action>
Update the same 5 keys in pt.json. This is European Portuguese (pt-PT), NOT Brazilian Portuguese. Read the finalized es.json and en.json for content reference, then write native European Portuguese copy.

**Critical pt-PT vocabulary rules:**
- Use "programador" (NOT "desenvolvedor" which is Brazilian for this context — though "desenvolvedor" is acceptable in some pt-PT contexts, "programador" is the safer pt-PT term for "developer")
- Use "utilizador" not "usuário"
- Use "ecrã" not "tela"
- Use European Portuguese verb conjugations and vocabulary throughout
- Formal-but-warm register appropriate for Portuguese professional communication

**1. SEO_TITLE_1:**
"Diego de Sousa | Programador Full-Stack & Designer de Produto"

**2. SEO_DESCRIPTION:**
Rewrite for Portuguese market (pt-PT). 150-160 characters. Use pt-PT SEO keywords: "programador full-stack", "designer de produto", "websites de alto desempenho", "aplicações SaaS". Include location: "Galiza, Espanha". First person.

**3. HERO.H1:**
Localize for European Portuguese. Keep `<span class='block'>` wrapper. Keep `<em>` tags. Portuguese register: professional-warm, slightly more formal than Spanish but not stiff.

**4. ABOUTSECTION.CONTENT:**
Write as native European Portuguese. Same narrative arc as Spanish/English (design-first, evolution to full-stack, two service lines, freelance, 10+ years, Galicia, accessibility, languages, CTA). Use pt-PT conventions throughout.

Language links with Portuguese labels:
- `<a href='/'><strong>espanhol</strong> 🇪🇸</a>`
- `<a href='/gl/'><strong>galego</strong> 🌳</a>`
- `<a href='/en/'><strong>inglês</strong></a> 🇬🇧`
- `<a href='/pt/'><strong>português</strong> 🇵🇹</a>`

Use same HTML tags consistently: `<p>`, `<strong>`, `<em>`, `<a>`.

**5. CONTACTSECTION.FORM.HELP_PLACEHOLDER:**
pt-PT version: include aplicações SaaS, dashboards alongside web design/development. Keep casual list format.

**Important:**
- Do NOT modify any other keys
- Preserve exact JSON structure and formatting (tabs for indentation)
- Must read as native European Portuguese — no Brazilian Portuguese artifacts, no translation artifacts
- Validate JSON syntax after editing
  </action>
  <verify>
Run: `node -e "const fs=require('fs'); const j=JSON.parse(fs.readFileSync('src/i18n/pt.json','utf8')); console.log('SEO_TITLE_1:', j.SEO_TITLE_1); console.log('SEO_DESC length:', j.SEO_DESCRIPTION.length); console.log('HERO.H1 has span:', j.HERO.H1.includes('<span')); console.log('ABOUT has p tags:', j.ABOUTSECTION.CONTENT.includes('<p>')); console.log('HELP:', j.CONTACTSECTION.FORM.HELP_PLACEHOLDER); console.log('JSON valid: true');"` — all checks must pass.
  </verify>
  <done>
- SEO_TITLE_1 contains pt-PT positioning
- SEO_DESCRIPTION is 150-160 characters, uses pt-PT keywords
- HERO.H1 reads as native European Portuguese
- ABOUTSECTION.CONTENT reads naturally in pt-PT, covers all required narrative elements
- HELP_PLACEHOLDER includes SaaS, aplicações, dashboards
- JSON parses without error
- No other keys modified
- No Brazilian Portuguese vocabulary used
  </done>
</task>

<task type="auto">
  <name>Task 2: Localize gl.json — Professional Galician profile content</name>
  <files>src/i18n/gl.json</files>
  <action>
Update the same 5 keys in gl.json. Galician must be treated as a fully professional language — not a cultural gesture or simplified variant of Spanish/Portuguese.

**Galician-specific vocabulary:**
- "desenvolvedor" is correct Galician for "developer"
- "deseñador" for "designer"
- "produto" for "product"
- "rendemento" for "performance"
- "aplicacións" for "applications"
- Use standard Galician (normativa) orthography
- Professional register matching the warmth of Spanish but in Galician conventions

**1. SEO_TITLE_1:**
"Diego de Sousa | Desenvolvedor Full-Stack & Deseñador de Produto"

**2. SEO_DESCRIPTION:**
Rewrite for Galician audience. 150-160 characters. Use Galician keywords: "desenvolvedor full-stack", "deseñador de produto", "webs de alto rendemento", "aplicacións SaaS". Include location: "Galicia, España". First person.

**3. HERO.H1:**
Localize for Galician. Keep `<span class='block'>` wrapper. Keep `<em>` tags. Galician tone: warm and professional, matching the register of the Spanish version adapted to Galician conventions.

**4. ABOUTSECTION.CONTENT:**
Write as native Galician. Same narrative arc (design-first, evolution to full-stack, two service lines, freelance, 10+ years, Galicia, accessibility, languages, CTA). This is Diego's home language region — the Galician version can carry a natural sense of rootedness.

Language links with Galician labels:
- `<a href='/'><strong>español</strong> 🇪🇸</a>`
- `<a href='/gl/'><strong>galego</strong> 🌳</a>`
- `<a href='/en/'><strong>inglés</strong></a> 🇬🇧`
- `<a href='/pt/'><strong>portugués</strong> 🇵🇹</a>`

Use same HTML tags consistently: `<p>`, `<strong>`, `<em>`, `<a>`.

**5. CONTACTSECTION.FORM.HELP_PLACEHOLDER:**
Galician version: include aplicacións SaaS, cadros de mando (dashboards) alongside web design/development. Keep casual list format.

**Important:**
- Do NOT modify any other keys
- Preserve exact JSON structure and formatting (tabs for indentation)
- Must read as professional, native Galician — not a castilianized version or reduced register
- Validate JSON syntax after editing
  </action>
  <verify>
Run: `node -e "const fs=require('fs'); const j=JSON.parse(fs.readFileSync('src/i18n/gl.json','utf8')); console.log('SEO_TITLE_1:', j.SEO_TITLE_1); console.log('SEO_DESC length:', j.SEO_DESCRIPTION.length); console.log('HERO.H1 has span:', j.HERO.H1.includes('<span')); console.log('ABOUT has p tags:', j.ABOUTSECTION.CONTENT.includes('<p>')); console.log('HELP:', j.CONTACTSECTION.FORM.HELP_PLACEHOLDER); console.log('JSON valid: true');"` — all checks must pass.
  </verify>
  <done>
- SEO_TITLE_1 contains Galician positioning ("Desenvolvedor Full-Stack & Deseñador de Produto")
- SEO_DESCRIPTION is 150-160 characters, uses Galician keywords
- HERO.H1 reads as native Galician
- ABOUTSECTION.CONTENT reads naturally in Galician, covers all required narrative elements
- HELP_PLACEHOLDER includes aplicacións SaaS, cadros de mando
- JSON parses without error
- No other keys modified
- Professional Galician register throughout
  </done>
</task>

<task type="auto">
  <name>Task 3: Validate all 4 JSON files and run build</name>
  <files>src/i18n/es.json, src/i18n/en.json, src/i18n/pt.json, src/i18n/gl.json</files>
  <action>
Perform comprehensive validation across all 4 language files:

**1. JSON syntax validation:**
Parse all 4 files with Node.js to confirm valid JSON:
```bash
node -e "['es','en','pt','gl'].forEach(l => { JSON.parse(require('fs').readFileSync('src/i18n/'+l+'.json','utf8')); console.log(l+'.json: valid') })"
```

**2. Key structure consistency:**
Extract all key paths from each file and compare. All 4 must have identical key structures:
```bash
node -e "
const fs = require('fs');
function getKeys(obj, prefix='') {
  return Object.entries(obj).flatMap(([k,v]) =>
    typeof v === 'object' && v !== null ? getKeys(v, prefix+k+'.') : [prefix+k]
  );
}
const langs = ['es','en','pt','gl'];
const keys = langs.map(l => getKeys(JSON.parse(fs.readFileSync('src/i18n/'+l+'.json','utf8'))));
const ref = JSON.stringify(keys[0].sort());
langs.forEach((l,i) => {
  const match = JSON.stringify(keys[i].sort()) === ref;
  console.log(l+'.json keys match es.json:', match);
  if (!match) { console.log('MISMATCH!'); process.exit(1); }
});
console.log('All key structures match');
"
```

**3. Content spot-checks:**
Verify the 5 target keys contain expected content patterns:
- SEO_TITLE_1 in each language contains "Full-Stack"
- SEO_DESCRIPTION in each language is between 100-200 characters
- HERO.H1 in each language contains `<span class='block'>`
- ABOUTSECTION.CONTENT in each language contains `<p>` tags
- HELP_PLACEHOLDER in each language mentions SaaS

**4. Build validation:**
Run `npm run build` to confirm the site builds successfully with updated content. This catches any JSON parse errors, missing key references, or HTML rendering issues.

**If any validation fails:** Fix the issue in the affected JSON file(s) before marking this task complete.
  </action>
  <verify>
Run `npm run build` — must complete without errors. All 4 JSON files must parse correctly and have identical key structures.
  </verify>
  <done>
- All 4 JSON files parse as valid JSON
- All 4 files have identical key structures
- All 5 target keys contain expected content in all 4 languages
- `npm run build` succeeds without errors
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. All 4 JSON files (es, en, pt, gl) parse as valid JSON
2. All 4 files have identical key structures (critical for fallback merge pattern)
3. `npm run build` succeeds
4. git diff shows changes only to the 5 target keys in each file
5. SEO_DESCRIPTION is under 160 characters in all languages
6. No translation artifacts — each language reads naturally
7. European Portuguese (not Brazilian) used in pt.json
8. Professional Galician (not castilianized) used in gl.json
</verification>

<success_criteria>
- pt.json updated with European Portuguese content for all 5 keys
- gl.json updated with professional Galician content for all 5 keys
- All 4 language files validated for JSON syntax and key structure consistency
- `npm run build` passes
- Phase 1 complete: all 5 requirements (SEO-01, SEO-02, HERO-01, ABOUT-01, CONT-01) satisfied across all 4 languages
</success_criteria>

<output>
After completion, create `.planning/phases/01-profile-content-update/01-02-SUMMARY.md`
</output>
